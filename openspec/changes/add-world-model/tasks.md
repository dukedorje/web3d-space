# Tasks

- [x] Typed world document (HDRI, GLB entities, cameras, lights, LOD
      distances, optional splat refs)
- [x] Loader: missing asset fails visibly
- [x] Fixture round-trip unit test

Handoffs (not boxes):

- `add-splat-cache` — binds splat URLs from the document
- `add-browser-canvas` — loads a world onto `/splat` or a canvas route
- `add-gpu-exec` — disjoint; no file overlap
